server:
  port: 8083
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_service?useUnicode=true&serverTimezone=UTC
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false
    hibernate.ddl-auto: update