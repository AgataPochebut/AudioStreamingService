server:
  port: 0
  servlet:
    context-path: /

spring:
  profiles:
    active: test

eureka:
  client:
    enabled: false

s3:
  defaultBucket: filestest
  tempBucket: temp

fs:
  defaultFolder: /Java/files
  tempFolder: /Java/temp

storage:
  type: FS