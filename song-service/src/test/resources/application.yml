server:
  port: 0
  servlet:
    context-path: /

eureka:
  client:
    enabled: false

s3:
  defaultBucket: filestest
  tempBucket: temp

fs:
  defaultFolder: /Java/files
  tempFolder: /Java/temp

spring:
  profiles:
    active: test