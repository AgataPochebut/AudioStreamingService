server:
  port: 0
  servlet:
    context-path: /

spring:
  profiles:
    active: test

  activemq:
    packages:
      trust-all: true

eureka:
  client:
    enabled: false

feign:
  hystrix:
    enabled: true

s3:
  defaultBucket: filestest
  tempBucket: temp

fs:
  defaultFolder: /Java/files
  tempFolder: /Java/temp

storage:
  type: FS